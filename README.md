# weightedERGM
All R code for two mode segmentation using ERGMs with nonparametric kernel based likelihood optimization for estimating the weights. Note that to run any function in this package, you would need to install the corresponding boost package containing C code in weightedERGM_C.
